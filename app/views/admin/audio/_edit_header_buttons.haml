.arl_admin_resource_header_buttons
  - if audio_neighbors.previous
    = button_to_previous(audio_neighbors.previous_path_admin)
  = button_to_show admin_audio_path(audio.id_public)
  = button_to_new new_admin_audio_path
  = button_to_index admin_audio_index_path
  - if audio_neighbors.next
    = button_to_next(audio_neighbors.next_path_admin)
