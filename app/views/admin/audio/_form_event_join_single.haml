.arl_form_title
  Add audio to event

- if selectable.does_have_events
  = form_with model: [:admin, audio], url: admin_audio_path(audio, pane: :events), local: true, class: :arl_form, method: :patch do |f|
    .arl_form_data
      = f.fields_for :event_audio, audio.event_audio.build do |aa|
        = render 'form_elements/collection_select', form: aa, attribute: :event_id, blank: true, collection: selectable.events, label: 'Title', text_method: :datetime_and_title
        = render 'form_elements/text_field_sized', form: aa, attribute: :event_order, label: 'Order', size: 4
    .arl_form_actions
      = f.submit class: :arl_button_form_submit

- else
  .arl_form
    .test_form_null No events.
