= form_with(model: [:admin, event], local: true, class: :arl_form) do |f|
  .arl_form_title
    Destroy event?
    .arl_form_data_label_destroy= "\"#{event.title}\""
    .arl_form_data_label_destroy= event.datetime

  %ul.test_form_explanation
    %li.test_form_explanation_warning This action can not be undone.

  .arl_form_actions
    = button_admin_submit_to_destroy admin_event_path(event.id), data: { confirm:  'Are you sure?' }, label: 'Destroy Event?'
