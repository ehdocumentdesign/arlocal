- if item.is_article
  = render 'show_info_item_article', article: item.infopageable
- elsif item.is_link
  = render 'show_info_item_link', link: item.infopageable
- elsif item.is_picture
  = render 'show_info_item_picture', picture: item.infopageable
