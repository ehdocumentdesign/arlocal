= form_with(model: [:admin, link], local: true, class: :arl_form) do |f|
  .arl_form_title
    Destroy link?
    .arl_form_data_label_destroy= tag.i link.name

  %ul.test_form_explanation
    %li.test_form_explanation_warning This action can not be undone.

  .arl_form_actions
    = button_admin_submit_to_destroy admin_link_path(link.id), data: { confirm:  'Are you sure?' }, label: 'Destroy Link?'
