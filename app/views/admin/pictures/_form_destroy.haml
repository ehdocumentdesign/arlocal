= form_with(model: [:admin, picture], local: true, class: :arl_form) do |f|
  .arl_form_title
    Destroy picture?
    .arl_form_data_label_destroy= tag.i picture.title

  %ul.test_form_explanation
    %li.test_form_explanation_caveat Picture will be removed from any albums or events.
    %li.test_form_explanation_caveat <em>Imported</em> picture files will remain in the file system.
    %li.test_form_explanation_caveat <em>Uploaded</em> picture attachment(s) will be purged.
  %ul.test_form_explanation
    %li.test_form_explanation_warning This action can not be undone.

  .arl_form_actions
    = button_admin_submit_to_destroy admin_picture_path(picture.id), data: { confirm:  'Are you sure?' }, label: 'Destroy Picture'
