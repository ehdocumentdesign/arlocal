.arl_admin_resource_joined
  .arl_admin_resource_joined_heading
    = resource_joined_albums_count_statement(resource)
  - if resource.does_have_albums
    .arl_admin_resource_joined_others
      - resource.albums.order_by_title_asc.each do |album|
        .arl_admin_resource_joined_other
          = album.title
